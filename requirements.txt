# python=3.10
streamlit>=1.28.0,<1.40.0
pandas>=2.0.0,<2.3.0
numpy>=1.24.0,<2.0.0
pdfplumber==0.11.4  # 固定版（Cloudで動作報告が多い安定版）
pillow==10.3.0      # pdfplumberの画像依存を明示的に固定
cryptography==42.0.5  # pdfminer経由の暗号化依存を固定
